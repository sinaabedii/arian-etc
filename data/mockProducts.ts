import { ProductDetail } from '@/types';

export const mockProductDetails: ProductDetail[] = [];

export const getProductBySlug = (_slug: string): ProductDetail | undefined => {
  return undefined;
};

export const getAllProductSlugs = (): string[] => {
  return [];
};
