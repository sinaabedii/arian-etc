# Force Next.js to use Babel instead of SWC
NEXT_FORCE_SWC=false
SWC_DISABLE=true

# Backend API Configuration
NEXT_PUBLIC_API_BASE_URL=http://172.20.20.66:8088

# Authentication Configuration
# Backend uses token-based authentication (Bearer tokens)
NEXT_PUBLIC_AUTH_SCHEME=Bearer
NEXT_PUBLIC_AUTH_USE_COOKIES=true
